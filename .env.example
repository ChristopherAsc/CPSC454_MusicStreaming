# if these aren't set, it'll use the default sqlite3 instead
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=   # if the databse doesn't work, just comment out this one to make the app use the default sqllite3 by default.
DB_PORT=3306
